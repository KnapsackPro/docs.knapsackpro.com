type: string
required: false
example: 'Jo** Do* <jo**.do*@ex*****.co*>'
description: |
  Who triggered the CI build, usually it's the commit author. It must be masked with a regexp pattern `/(?<=\w{2})[a-zA-Z]/g`. For example, `John Doe <john.doe@example.com>` should be masked as in the following example.
