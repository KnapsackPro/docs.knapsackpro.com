type: string
required: true
example: Jo** Do* <jo**.do*@ex*****.co*>
description: |
  A user name that started the CI build. It is usually the same person that made the git commit. It must be masked with a regexp pattern `/(?<=\w{2})[a-zA-Z]/g`. For example, `John Doe <john.doe@example.com>` should be masked as in the following example.
